{% extends 'frame.html' %}

{% block "content" %}

<h1>Meetings Index</h1>
<h5>Click on a meeting to get started</h5>
{% if object_list %}
<table class='table table-hover table-bordered'>
    <thead>
       <tr>
           <th>Meeting Name</th>
           <th>Meeting Type</th>
           <th>Meeting Date</th>
       </tr>
    </thead>
    <tbody>
    {% for meeting in object_list %}
        {% if meeting.is_editable %}
            <tr>
            <td><a href="{% url 'meetings:edit' meeting.pk %}">
            {{ meeting.name }}</a></td>
            <td>{{ meeting.meeting_type }}</td>
            <td>{{ meeting.meeting_date }}</td>
            <tr>
        {% else %}
            <tr>
            <td><a href="{% url 'meetings:detail' meeting.pk %}">
            {{ meeting.name }}</a></td>
            <td>{{ meeting.meeting_type }}</td>
            <td>{{ meeting.meeting_date }}</td>
            </a>
            <tr>
        {% endif %}
    {% endfor %}
</table>
{% else %}
    <p>No meetings yet.</p>
{% endif %}

<a href="{% url 'meetings:create' %}">Create a New Meeting</a>

{% endblock %}
